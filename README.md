## 

```text

issues
    session is stored in MemoryStore
        store session in dedicated session store
            reddis
    hard-coded dummy information is causing confusion
        dynamically generate random value in all places where a unique random value must be generated

```

